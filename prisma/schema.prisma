datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

generator client {
  provider      = "prisma-client-js"
  binaryTargets = ["native"] 
}

model Usuario {
  id      String    @id   @default(uuid())
  name    String
  email   String
  phone   String

  @@map("usuarios")
}
